import { WebpBackgroundDirective } from './webp-background.directive';

describe('WebpDirective', () => {
  it('should create an instance', () => {
    // const directive = new WebpBackgroundDirective();
    // expect(directive).toBeTruthy();
  });
});
