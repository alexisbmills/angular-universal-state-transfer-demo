import { WebpImageDirective } from './webp-image.directive';

describe('WebpImageDirective', () => {
  it('should create an instance', () => {
    const directive = new WebpImageDirective();
    expect(directive).toBeTruthy();
  });
});
